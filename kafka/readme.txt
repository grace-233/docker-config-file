注意：
kafka集群搭建的时候，故意将容器内的端口和宿主机端口设置为一样。
因为在用kafka客户端连接kafka集群的时候,获取的是容器内的主机名和端口号。比如kafka2:9092。
这样的话，默认情况下是连不上容器内的端口的。
所以在容器内使用9003端口，映射到宿主机的9093端口，并把kafka2和宿主机ip配置到client端的hosts里面，
这样客户端连接kafka主机kafka2:9003,实际上连接的就是宿主机的端口和ip,就能连上kafka集群了